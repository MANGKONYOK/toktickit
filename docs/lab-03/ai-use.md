# Lab 3 — AI Use and Reflection

**LLM / Agent used:** Antigravity (Gemini 3.8 Flash)

---

## Selected Key Prompts

| # | Prompt (Summarised) | What I did with the result |
|---|---------------------|----------------------------|
| 1 | Analyze the CPE 334 Lab 3 labsheet, identify sprint scope boundaries, define the 7-feature breakdown, and formulate the role authorization matrix across `REQUESTER`, `IT_STAFF`, and `ADMIN`. | Established the exact scope boundaries (e.g., real email/password authentication, 3 roles, mandatory first-login password change, IT staff queue, internal notes, minimalist admin user management; strictly excluding self-registration, magic links, SMTP delivery, MFA, and hard user deletion) and mapped out the 7 git feature branches and PR pipeline targeting `lab3-staging`.<br>**My Reflection:** Establishing strict scope boundaries and role definitions prior to development prevents feature creep and ensures that security boundaries are treated as hard contracts rather than afterthoughts. |
| 2 | Author the Sprint 3 engineering contract deliverables in `docs/lab-03/` (`specification.md`, `api-spec.md`, `ui-spec.md`, `tests.md`), detailing all 15 Functional Requirements, 25 Business Rules, 22 Acceptance Criteria, 8 permitted status transitions, and data schema evolution. | Created the complete set of contract documents. Defined the `User`, `Role`, `Comment`, and `InternalNote` data models, the exact 8 allowed ticket status transitions (`NEW` $\to$ `OPEN`, `CANCELLED`; `OPEN` $\to$ `IN_PROGRESS`, `WAITING_FOR_REQUESTER`, `RESOLVED`, `CANCELLED`, etc.), the strict confidentiality rules for Internal Notes (HTTP 403 for Requesters), and the 6-tier test architecture with AC traceability.<br>**My Reflection:** Specifying the API contracts and status transition rules upfront eliminates ambiguities during backend implementation and ensures the server-side state machine cannot be bypassed by client-side shortcuts. |
| 3 | Plan and execute Feature 2: evolve Prisma schema (migrate `RequesterUser` into `User`, add `Role` enum, `Comment`, and `InternalNote` tables), update idempotent seed with 9 bcrypt-hashed accounts, implement password complexity validation (`UNIT-01`), server auth endpoints (`POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me`, `POST /api/auth/change-password`), RBAC middleware (`requireAuth`, `requirePasswordChangeClear`, `requireRole`), and client UI components (`Login.tsx`, `ChangePasswordModal.tsx`, `Navbar.tsx`). | Applied structured Prisma schema migration without data loss, implemented anti-enumeration defense ordering (verifying password before checking account active status to prevent account enumeration leaks), built the session cookie infrastructure with `cookie-parser`, developed the mandatory password change gate and real-time complexity checklist, and verified all 24 new Lab 3 tests plus all 61 existing Lab 2 regression tests pass 100%.<br>**My Reflection:** Implementing the password verification prior to account status checking is critical for security: revealing that an account is deactivated before checking the password leaks whether an email address is registered in the system. Enforcing backward compatibility with Lab 2 simulated requester context while introducing real session-based RBAC allows progressive delivery without breaking existing regression test suites. |
| 4 | Address peer review findings from @kmood-Sakura on PR #2 (`lab3-feature/2-auth-foundation`): eliminate unsigned cookie fallback and enforce HMAC-SHA256 Bearer token signature verification in `auth.ts`, resolve Lab 2 ticket requester lookups against `User` table with fallback, make seed genuinely idempotent without wiping user updates/passwords or hard deleting, add secure cookie flags in production, and synchronize `specification.md` schema documentation. | Updated `middleware/auth.ts` to reject unsigned `req.cookies` and strictly require `req.signedCookies` or cryptographic HMAC tokens via `signBearerToken`/`verifyBearerToken`; added `findActiveRequester` in `app.ts` to query `User` then `RequesterUser`; revised `seed.ts` to preserve `passwordHash`, `mustChangePassword`, and `isActive` on update and synchronized initial user sequence 1:1; updated `specification.md` §8.1 to match database column names; added automated security tests verifying unsigned cookie and forged token rejection; all 88 server and 45 client tests pass.<br>**My Reflection:** Peer code review is indispensable for catching subtle security loopholes—such as trusting unsigned cookies in development middleware fallbacks or subtle sequence drifts between migrated relational tables. Ensuring that database seeds are genuinely idempotent protects persistent test and operational states across repeated deployments. |
| 5 | Plan and execute Feature 3: implement authenticated requester ticket creation bound strictly to session identity (`req.user.id`), ticket ownership isolation returning HTTP 404 on unowned tickets, public comments stream (`POST/GET /api/tickets/:id/comments`), requester problem resolution indication (`PATCH /api/tickets/:id/resolve-indication`), first-login password change interception across ticket endpoints, UI discussion thread with author role badges, and comprehensive test suites preserving Lab 2 regression compatibility. | Implemented server-side ticket binding and ownership checks in `server/src/app.ts`, created public comment endpoints with input validation (1..2000 chars) and role-gated access, implemented `PATCH /api/tickets/:id/resolve-indication` setting `resolvedByRequester = true` without altering operational status per BR-05, updated `TicketDetail.tsx` with live public comments stream and "✓ Problem Appears Resolved" toggle/badge, updated `CreateTicket.tsx` and `MyTickets.tsx` with `useAuth`, and created 19 new tests (`API-10..14`, `UI-04`) with 100% pass rate across 103 server tests and 49 client tests.<br>**My Reflection:** Preserving multi-tenant isolation by returning HTTP 404 rather than 403 on unowned resources prevents ID enumeration attacks by eliminating existence leakage. Designing the problem resolved indication as an explicit requester signal without altering the IT operational status honors the separation of concerns between requester satisfaction and formal IT SLA lifecycle governance. |
| 6 | Address peer review findings from @kmood-Sakura on PR #3 (`lab3-feature/3-requester-continuity`): enforce `requireAuth` on all ticket and attachment routes returning HTTP 401 Unauthorized for unauthenticated calls and deprecating legacy `x-requester-id` headers per FR-05 & BR-06, fix table formatting in `docs/lab-03/tests.md` line 37 separating `API-09` and `API-10` into distinct rows, and align `PATCH /api/tickets/:id/resolve-indication` with the RBAC matrix by checking requester role first to return HTTP 403 Forbidden for non-requesters (`IT_STAFF`, `ADMIN`). | Updated `server/src/app.ts` to require authentication across all ticket and attachment endpoints, rejecting unauthenticated requests with 401 and strictly deriving ticket identity from `req.user.id`; updated `PATCH /api/tickets/:id/resolve-indication` to verify role before ownership (returning 403 for Staff/Admin per specification.md:133); split line 37 in `tests.md` into distinct rows restoring Table 2 rendering and verifying AC-08; aligned all Lab 2 test suites using cryptographic HMAC tokens (`signBearerToken`) and added regression tests for 401 unauthenticated and 403 role rejections; verified 100% pass across 109 server tests and 49 client tests.<br>**My Reflection:** Enforcing strict authentication at the route handler layer eliminates ambient authority and ensures that identity cannot be spoofed through leftover legacy headers. Checking authorization roles prior to resource existence or ownership is essential when the capability contract specifies explicit denial (403), ensuring the system's external behavior strictly mirrors the approved RBAC matrix. |

*(Prompts 7 through 10 will be recorded incrementally as Features 4 through 7 are planned and implemented step-by-step).*