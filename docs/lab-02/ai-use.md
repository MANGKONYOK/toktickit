# Lab 2 - AI Use and Reflection

**LLM / Agent used:** Antigravity (Gemini 3.7 Flash)

## Selected Key Prompts (6-10)

| # | Prompt (Summarised) | What I did with the result |
|---|---------------------|----------------------------|
| 1 | Summarize Lab 2 requirements, strict scope boundaries, issue dependencies and test specifications | Adopted the structured breakdown of the 6 sprint features, documentation files and Git Flow staging strategy.<br>**My Reflection:** Understanding project scope boundaries and Spec-Driven Development is essential before writing any production code. |
| 2 | Create Sprint 2 engineering contract and blank documentation templates in `docs/lab-02/` | Adopted the required submission sections and established the full specification documents (`specification.md`, `api-spec.md`, `ui-spec.md`, `tests.md`, `reviewer.md`, `ai-use.md`).<br>**My Reflection:** Setting up clear specifications upfront prevents architectural drift and clarifies acceptance criteria early. |
| 3 | Plan and implement Feature 2: Development Requester testing context, PostgreSQL seed, reference APIs, and selector UI | Scaffolded `RequesterUser` and `RelatedSystem` Prisma models, wrote idempotent seed data with upsert, implemented `/api/requesters` and `/api/related-systems`, created Zen Green `RequesterContext` with localStorage persistence, and wrote automated unit and Supertest suites.<br>**My Reflection:** Implementing the simulated requester testing context strictly with `isActive: true` filtering prevents unauthorized or deleted users from being selected, and keeping the state in React Context cleanly isolates development simulation from production auth. |
| 4 | Plan and implement Feature 3: Create Ticket Form, Sequential Ticket Number Generator (`TKT-YYYY-NNNNNN`), and Field Validations | Scaffolded `Ticket` and `TicketSequence` Prisma models with migration, built transactional `generateTicketNumber` and validator functions, implemented `POST /api/tickets` with 400/404/201 handling, created Zen Green `CreateTicket` form with inline field errors, busy states, and form preservation, and verified all unit/Supertest/Vitest suites.<br>**My Reflection:** Enforcing atomic sequence generation in database transactions prevents duplicate ticket numbers under concurrent load, and inline client validation combined with strict server-side validation ensures defense-in-depth. |
| 5 | Plan and implement Feature 4: My Tickets View (Search, Multi-Filter, Sorting, Pagination & User Data Isolation) integrating Lab 2 baseline, POS prompts companion, and agents_md engineering contracts | Built pure query parser with strict validation (`ticket-query.ts`), implemented `GET /api/tickets` with case-insensitive search, multi-filter combinations, column sorting, pagination, and multi-user ownership isolation (`where: { requesterId }`). Created responsive Zen Green `MyTickets.tsx` with filter toolbar, desktop table, mobile stacked cards, dual empty states (`AC-11`), and pagination controls. Verified with 16 new API tests (`my-tickets.api.test.ts`), 5 component tests (`MyTickets.test.tsx`), and 2 responsive tests (`ResponsiveLayout.test.tsx`).<br>**My Reflection:** Enforcing ownership data isolation directly at the database query level guarantees that requesters cannot access other users' tickets even if query parameters are manipulated. Providing distinct empty states between a truly empty queue and filtered-out results significantly improves user orientation. |
| 6 | Plan and implement Feature 5: Ticket Detail View, Attachment Uploads, Soft-Removal Lifecycle, and Download Blocking | Added `Attachment` Prisma model, configured Multer for 5MB and allowed MIME types, implemented Endpoints 6-10 (`GET /api/tickets/:id`, upload, list, download, soft-remove), created `TicketDetail.tsx` with read-only `#F0F4F1` surface and `AttachmentSection.tsx` with modal removal reason validation ($\ge 5$ chars), and verified download blocking.<br>**My Reflection:** Enforcing soft-removal with mandatory reasons provides a complete audit trail without physically destroying records, while blocking binary downloads protects sensitive information from unauthorized leakage. |
| 7 | Plan and implement Feature 6: End-to-End User Journeys and Cross-Browser Responsive Verification | Built the Zen Green design system conformance test suite (`ZenGreenStyle.test.tsx` verifying color tokens `#006B3C`, `#0B7A46`, `#EAF6EF`, `#F0F4F1`, `#C5221F`, mandatory red asterisks, and 44px touch targets), configured multi-project Playwright automation (`playwright.config.ts`), implemented the full end-to-end user journey across all 8 test journeys (`requester-ticket-flow.spec.ts`), captured all 19 visual inspection screenshot artifacts across desktop, tablet, and mobile viewports, and completed all verification documentation.<br>**My Reflection:** Automated cross-browser and multi-viewport testing (Desktop 1280x800, Tablet 768x1024, Mobile 375x667) guarantees that responsive transformations—such as switching from a desktop data table to mobile stacked cards—function seamlessly under actual user input while strictly preserving Zen Green touch target sizing (>= 44px) and zero horizontal overflow. |
| 8 | Resolve partner review feedback on PR #21 (Feature 5: Identity precedence, MIME/extension cross-check, and query-level predicate isolation) | Updated Endpoints 6–10 in `server/src/app.ts` to strictly prioritize `req.headers["x-requester-id"]` over query/body parameters, preventing privilege escalation. Enhanced Multer file filter in `server/src/utils/upload.ts` to validate file extension alignment with declared MIME types, rejecting disguised executables (`trojan.exe` sent as `image/png`) with HTTP 415. Enforced Prisma `where: { requesterId }` predicates on ticket detail and attachment operations, and added anti-spoofing regression tests in `attachments.api.test.ts` and `ticket-detail.api.test.ts`.<br>**My Reflection:** Relying on client-submitted body or query parameters for identity creates severe authorization vulnerabilities, especially on destructive endpoints like attachment soft-removal. Validating file extensions alongside MIME types prevents attackers from bypassing upload restrictions with spoofed headers. |
| 9 | Resolve peer review blockers and warnings on PR #22 (Feature 6: Dynamic ownership isolation in E2E, authentic screenshot timing, and mobile layout polish) | Refactored Playwright E2E Test 08 to query Sorawit's tickets (`GET /api/tickets`), extract his generated ticket ID, verify legitimate `200 OK` access, and assert strict `404 Not Found` with `{ error: { code: "TICKET_NOT_FOUND" } }` when requested as Piti. Added route delay interception to capture genuine submitting busy states with spinner, captured upload form and active attachment list distinctly, added Playwright `trace: "retain-on-failure"`, strengthened `STYLE-01.4` to check CSS `min-height: 44px`, and asserted zero horizontal overflow across Desktop, Tablet, and Mobile viewports.<br>**My Reflection:** E2E security assertions must dynamically prove ownership against actual database state rather than hardcoding static IDs that could pass on an empty database. Using network route delays allows automated suites to reliably capture transient UI states without relying on brittle fixed sleep timers. |
| 10 | Synthesize and record comprehensive peer review interactions across all sprint PRs (#10–#22) for both team members into `reviewer.md` | Queried the GitHub REST API using authenticated Git credentials to systematically retrieve full review comment threads, inline code suggestions, reviewer verdicts, and author responses for PRs #10, #11, #16, #17, #18, #19, #20, #21, and #22. Formatted all commendations, architectural blockers, and technical resolutions into structured Markdown tables and conversation blocks in `docs/lab-02/reviewer.md`.<br>**My Reflection:** Automating the retrieval and synthesis of peer review records ensures complete traceability across sprint features, eliminates human omission or distortion, and accurately documents collaborative engineering discussions and design trade-offs. |